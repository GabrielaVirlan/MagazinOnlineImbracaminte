@model IEnumerable<MagazinOnlineImbracaminte.Models.Cart>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                <a asp-controller="Cart" asp-action="Remove" asp-route-id="@item"> </a>
            </td>
            <td>
                
            </td>
        </tr>
}
    </tbody>
</table>
